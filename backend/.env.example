DATABASE_URL=sqlite:///./data/valuer.db
# Run: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=replace-with-at-least-32-random-characters
DATA_DIR=./data
ALLOW_REGISTRATION=true
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
GOOGLE_MAPS_API_KEY=
GOOGLE_MAPS_SIGNING_SECRET=
# Enable ONLY after checking your permission to embed and retain maps in reports.
MAP_REPORT_EXPORT_ALLOWED=false
SOFFICE_PATH=soffice
